module.exports = class Player {

    constructor(id, name, local, global, context) {
        this.id = id
        this.name = name
        this.local = local
        this.global = global
        this.context = context
    }

}
